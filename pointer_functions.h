#ifndef  POINTERS_H
#define POINTERS_H
extern int findAddress_of_numbers(int *ptr);
//Swap two numbers using pointers
extern void swaptwoNumbers(int *first, int *second);
//Find max/min using pointer in array
extern int* findMinMax(int *datas,int first);
//Dynamic memory allocation for array
extern int* dynamic_allocation_of_array(int length);
//String length using pointer
extern int find_string_length(char* str);
//Copy string using pointer
extern char* copy_string(char* str);
//search char from string
extern char search_string(const char* str,const char* search);
//Pointer to structure example
//Menu-driven calculator using function pointers
//Matrix input/output using pointer to pointer

#endif

